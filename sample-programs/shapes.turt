# move to the start position
rotate(-90);
setPos(screenWidth / 2 + 50, screenHeight / 2);

for (var i = 3; i <= 8; i = i + 1) {
    for (var j = 0; j < i; j = j + 1) {
        moveFwd(100);
        rotate(360 / i);
    }
}

hideTurtle();